# Analyse de Données avec Python pour Professionnels de la Santé
# Version condensée 6 heures
#
# Installation locale (alternative a Anaconda) :
#   pip install -r requirements.txt
#
# Versions minimales testees avec succes en juin 2026 (Python 3.10+).
# Les contraintes hautes evitent les ruptures futures sans bloquer les
# correctifs de securite.

# --- Pile scientifique de base ----------------------------------------
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0

# --- Visualisation -----------------------------------------------------
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# --- Statistique et machine learning -----------------------------------
# IMPORTANT : statsmodels >= 0.14.6. Les versions <= 0.14.5 cassent avec
# scipy >= 1.17 (suppression du symbole prive _lazywhere).
statsmodels>=0.14.6,<1.0
scikit-learn>=1.5,<2.0
lifelines>=0.30,<1.0

# --- Jupyter -----------------------------------------------------------
jupyter>=1.0
notebook>=7.0
ipykernel>=6.0

# --- Optionnel : visualisation de manquements --------------------------
# Le Module 2 utilise missingno si disponible, sinon une visualisation
# matplotlib en repli. Decommenter si vous voulez missingno :
#
# missingno>=0.5
